# Project: shy-plants-vs-zombies-new
# Makefile created by Red Panda C++ 1.0.6

CPP      = clang++.exe
CC       = clang.exe
WINDRES  = windres.exe
OBJ      = main.o
LINKOBJ  = main.o
CLEANOBJ  =  main.o shy-plants-vs-zombies-new.exe
LIBS     =  -s -mwindows -static -static-libstdc++ -static-libgcc -lgraphics -luuid -lmsimg32 -lgdi32 -limm32 -lole32 -loleaut32 -lwinmm -lgdiplus   -static
INCS     =  
CXXINCS  =  
BIN      = shy-plants-vs-zombies-new.exe
CXXFLAGS = $(CXXINCS)  -O2 -pipe
CFLAGS   = $(INCS)  -O2 -pipe
RM       = del /q /f

.PHONY: all all-before all-after clean clean-custom

all: all-before $(BIN) all-after

clean: clean-custom
	${RM} $(CLEANOBJ) > NUL 2>&1

$(BIN): $(OBJ)
	$(CPP) $(LINKOBJ) -o $(BIN) $(LIBS)

main.o: main.cpp
	$(CPP) -c D:/00000project/shy-plants-vs-zombies-new/main.cpp -o main.o $(CXXFLAGS) 
